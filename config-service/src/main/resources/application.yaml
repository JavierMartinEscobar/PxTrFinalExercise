server:
  port: 8081

application:
  name: config-service

spring:
  cloud:
    config:
      server:
        git:
          default-label: develop_m
          uri: https://github.com/JavierMartinEscobar/PxTrFinalExercise
          search-paths: config-data